// note: "class=" means "hasClass"

// open website
https://www.drugbank.ca/

read {nome_do_farmaco} // array of strings (maybe?)
foreach {nome_do_farmaco}

-- inside loop:

// fill search input
form class=home-search-form > input id=query name=query class=search-query
write {nome_do_farmaco}

// do the search
submit form // no submit button found

// check if the search was positive (info page instead of a list of suggestions)
if {nome_do_farmaco} == 
	content from: 
		main > div class=content-container 
			 > div class=content-header > h1
then found = true

// get vars from "PROPERTIES > Predicted Properties" table
if found {
	get table id=drug-moldb-properties > tbody 
			> tr > td:nth-child(2) // VALUE
	where table id=drug-moldb-properties > tbody 
			> tr > td:nth-child(1) // PROPERTY
	eq [Water Solubility|Density]
}

// just go back to start all over again
back()

-- repeat the loop
