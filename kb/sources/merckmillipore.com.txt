// note: "class=" means "hasClass"

// open website
https://www.merckmillipore.com/BR/pt

vars = [
	Celulose microcristalina, 
	Polivinilpirrolidona, 
	Croscarmelose sódica, 
	Aerosil, 
	Estearato de magnésio, 
	Fosfato de cálcio,
	Lactose monohidratada
]

foreach vars as var

-- inside loop:

// fill search input
form id=searchForm > input class=searchBox
	id=searchTerm_2RGb\.qB\.zxsAAAFPUml1gL3H // not sure if id is reliable
write {var}

// do the search
click form id=searchForm > button name=search || submit the form

// if more than one result
if content of 
	section > nav class=breadcrumbs > ul > li:last-child.hasClass("active") 
	== "Pesquisar" {
	
	// get first result
	click div class=search-result-tabs > div class=panel-container 
		> section id=Products > div:last-child class=omega
		> section:first-child class=product first
		> div:nth-child(1) class=container-serp
		> h2 class=search-r-title
		> a:first-child class=product-link

}

// if it is the result page indeed
if content of
	section > nav class=breadcrumbs > ul > li:last-child.hasClass("active") 
		== "{var}" {

	// read cas
	read table id=keySpecMainTable > tbody > tr:first-child > td:first-child
	// can check if table > thead > tr > th:first-child == "No. CAS"

	// read info
	table id=attribute_Group_table_header_pdp_PDP_OverviewTab_Product_Physico_Chemical_Info
	// not sure if the table id is reliable as well
	// can check if table > thead > tr > th == "Informações fisico-químicas"
		> tbody > foreach tr:
			if td:first-child == "Solubilidade" || contains(Densidade) {
				read td:last-child // the info
			}

} else {
	// error
}

-- next loop

